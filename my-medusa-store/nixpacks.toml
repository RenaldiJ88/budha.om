[phases.setup]
nixPkgs = { nodejs = "20.x" }

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = [
  "chmod +x scripts/railway-start.sh || true",
  "echo 'Build phase: Script prepared for runtime'"
]

[start]
cmd = "./scripts/railway-start.sh"